package cs5004.animator.model;

/**
 * Enum of move types, helps to check move types assigned to a command as well as make it easier to
 * expand on later on.
 */
public enum MotionType {
  MOVE, SCALE, RECOLOR
}
